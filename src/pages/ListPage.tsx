import React from 'react';
import Layout from '../components/layout/Layout';

const ListPage = () => {
    return (
        <Layout>
            ListPage 
        </Layout>
    );
};

export default ListPage;